\chapter{Programa de adquisición de datos en Arduino}
\label{apen1}
bla bla bla bla bla bla bla bla bla ...

\begin{lstlisting}[label=hola1, caption=Hola Mundo en lenguaje C, language=C]
/* Autor: Merino Fernando
 * Fecha: 6 febrero 2015
 * Descripción: Imprime "Hola mundo desde Latex...!" N veces. Actualizado ahora usando Listings, únicamente listings, no es necesario minted, aunque si se requiere una interfaz aún más bonita, sería útil instalarlo.
*/
#include <stdio.h>
#define N 10
int main(){
	int i;
	// Línea de comentario.
	printf("Hola mundo desde Latex...!");
	for (i = 0; i < N; i++){
		printf("LaTeX es para programadores más sofisticados! LaTeX es para programadores más sofisticados! LaTeX es para programadores más sofisticados!");
	}
	
	for (i = 0; i < N; i++){
		printf("LaTeX es para programadores más sofisticados!
	}
	
	for (i = 0; i < N; i++){
		printf("LaTeX es para programadores más sofisticados!
	}
	return 0;
}
\end{lstlisting}